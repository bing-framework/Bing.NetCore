# 2026-09-17

## 生成 Bing.NetCore 框架文档
- 为项目生成两类文档（中文，UTF-8），放置于 `docs/使用文档.md` 与 `docs/设计思路文档.md`。
- 文档内容基于对 `framework/src`、`modules/admin`、`samples` 的源码探查，并经抽查核对（ApiResult 属性、StatusCode 枚举、IRepository.GetUnitOfWork、AddSqlServerUnitOfWork 签名等）。
- 关键事实（写文档时确认）：统一响应为 `ApiResult`(命名空间 Bing.AspNetCore.Mvc，物理位于 Bing.AspNetCore 工程) + `ResultHandlerAttribute` 全局过滤器；无 `UnifyResult`/`[Result]`。`IRepository`/`IStore`/`IQueryStore` 定义在 `Bing.Ddd.Domain`。EF Core 运行时命名空间为 `Bing.Datas.EntityFramework.Core`。多 ORM：EF Core(5 Provider)/FreeSQL(仅 MySql)/Dapper(5 Provider, 不提供仓储)。注册入口如 `AddSqlServerUnitOfWork<TService,TImpl>(conn,...)`、`AddJwt`(读 "JwtOptions", 策略名 "jwt")、`AddBingLogging`、`AddDefaultEventBus`/`AddCapEventBus`、`AddMailKit`、`AddRedisDistributedLock`、`AddAutoMapper`。缓存按约定 `RedisHelper.Initialization` + `AddScoped<ICache,...>`，无 AddCache。
- 注意事项：仓库 README 的“开发环境”章节 SDK 版本已过时，实际以 `global.json`(SDK 8.0.424) 与 net6.0 运行目标为准。

## 补充：架构与子系统深挖（续前次文档工作）
- 新增 `docs/架构文档.md`（纯结构视角：六层图、模块依赖、契约↔实现边界、部署形态、参考实现指针）。
- 新增 `docs/子系统深挖.md`（代码级深挖三子系统，均经源码复查）。本次复查**修正了早前笔记的几处偏差**：
  - CAP Outbox 的 `AdminUnitOfWork.SaveChangesAsync` 包装在 **MySql** 管理模块（`modules/admin/.../MySql/AdminUnitOfWork.cs`），非 SqlServer；`Bing.EntityFrameworkCore.SqlServer` 无此 UoW。
  - 事件总线抽象接口物理位于 `Bing.Core`（非独立 Abstractions）；订阅者用 `ICapSubscribe` + `[EventHandler]`(继承 CAP `TopicAttribute`)，**不用** `[CapSubscribe]`。
  - `IUnitOfWork`（在 `Bing.Uow`）仅声明 `Commit()`/`CommitAsync()`；`SaveChangesAsync`/`RollbackAsync` 来自 FreeSQL `DbContext` 基类，不在该接口。
  - `Bing.Data.Sql` 路由核心是**具体类 `SqlProviderRuntime`**（无 `ISqlProviderRuntime` 接口、无 `SqlProviderBase`）；DI 接口为 `ISqlQueryFactory`。Provider Key：bing.sqlserver/bing.mysql/bing.postgresql/bing.oracle/bing.sqlite；Doris 复用 bing.mysql。`DatabaseType` 含 6 值。
- 用 Visualizer 渲染了两张内联 SVG：分层架构图、写路径+事件 Outbox 数据流图（浅色主题）。

## 补充2：继续深挖架构文档（晚间）
- 在 `docs/架构文档.md` 新增/修正：§2 模块依赖图改为基于真实 `references.props`/`dependency.props` 的工程引用表；新增 §8 模块系统与启动序列、§9 约定式 DI 自动注册、§10 读路径与异常管道（代码级）。
- 本轮复查修正的架构认知：模块启用顺序**仅按 Level→Order→FullName**，**不做基于 [DependsOnModule] 的拓扑排序**（依赖仅决定“包含哪些模块”）；`[Dependency]` 特性优先级高于 *Dependency 接口；约定 DI 注册类型实现的全部接口；`IAsyncQueryableProvider` 的 EF Core 实现真正落库，FreeSQL 的 `FreeSqlAsyncQueryableProvider.CanExecute=false` 为占位；异常响应头常量 `_BingErrorFormat`（非 Bing-Error-Format）；`Bing.Permissions` 反向依赖 `Bing.AspNetCore`。
- 真实存在的抽象工程仅：Bing.Validation.Abstractions / Bing.Localization.Abstractions / Bing.Auditing.Contracts / Bing.MultiTenancy.Abstractions / Bing.AspNetCore.Abstractions；Security/Permissions/ExceptionHandling/Caching 无独立 *Abstractions 工程。
- 用 Visualizer 渲染 3 张内联 SVG：模块启动序列、读路径、异常与统一响应管道（浅色主题）。

## 补充3：架构文档加 §11 模块清单
- 在 `docs/架构文档.md` 追加 §11「模块清单（每个工程一句话职责）」，按层分组列出 framework/src 下全部框架工程（地基与核心/领域层/数据访问实现层/表现层/事件与扩展）的一句话职责，命名空间与依赖边指向 §2。
- 说明业务模块在 modules/admin（Bing.Admin.*），示例在 samples/*，不在框架工程清单内。
